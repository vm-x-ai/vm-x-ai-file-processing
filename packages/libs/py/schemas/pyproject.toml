[tool.coverage.run]
branch = true
source = [ "dm_schemas" ]

[tool.coverage.report]
exclude_lines = ['if TYPE_CHECKING:']
show_missing = true

[tool.pytest.ini_options]
addopts = """
 --cov
 --cov-report html:'../../../../coverage/packages/libs/py/schemas/html'
 --cov-report xml:'../../../../coverage/packages/libs/py/schemas/coverage.xml'
 --html='../../../../reports/packages/libs/py/schemas/unittests/html/index.html'
 --junitxml='../../../../reports/packages/libs/py/schemas/unittests/junit.xml'
"""

[project]
name = "dm-py-schemas"
version = "1.0.0"
description = "Automatically generated by Nx."
requires-python = ">=3.9,<4"
readme = 'README.md'
dependencies = [
    "dm-py-db-models",
    "pydantic>=2.11.4",
]

[tool.hatch.build.targets.wheel]
packages = ["dm_schemas"]

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
exclude = [
  ".ruff_cache",
  ".svn",
  ".tox",
  ".venv",
  "dist",
]

line-length = 88
indent-width = 4

[tool.ruff.lint]
select = [
  # pycodestyle
  "E",
  # Pyflakes
  "F",
  # pyupgrade
  "UP",
  # flake8-bugbear
  "B",
  # flake8-simplify
  "SIM",
  # isort
  "I",
]
ignore = []

fixable = ["ALL"]
unfixable = []

[tool.uv.sources]
dm-py-db-models = { workspace = true }
