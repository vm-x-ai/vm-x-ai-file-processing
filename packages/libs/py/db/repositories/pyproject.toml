[tool.coverage.run]
branch = true
source = [ "vmxfp_db_repositories" ]

[tool.coverage.report]
exclude_lines = ['if TYPE_CHECKING:']
show_missing = true

[tool.pytest.ini_options]
addopts = """
 --cov
 --cov-report html:'../../../../../coverage/packages/libs/py/db/repositories/html'
 --cov-report xml:'../../../../../coverage/packages/libs/py/db/repositories/coverage.xml'
 --html='../../../../../reports/packages/libs/py/db/repositories/unittests/html/index.html'
 --junitxml='../../../../../reports/packages/libs/py/db/repositories/unittests/junit.xml'
"""

[project]
name = "vmxfp-py-db-repositories"
version = "1.0.0"
description = "Automatically generated by Nx."
requires-python = ">=3.9,<4"
readme = 'README.md'
dependencies = [
    "vmxfp-py-db-models",
    "vmxfp-py-db-services",
    "vmxfp-py-utils",
]

[tool.hatch.build.targets.wheel]
packages = ["vmxfp_db_repositories"]

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
exclude = [
  ".ruff_cache",
  ".svn",
  ".tox",
  ".venv",
  "dist",
]

line-length = 88
indent-width = 4

[tool.ruff.lint]
select = [
  # pycodestyle
  "E",
  # Pyflakes
  "F",
  # pyupgrade
  "UP",
  # flake8-bugbear
  "B",
  # flake8-simplify
  "SIM",
  # isort
  "I",
]
ignore = []

fixable = ["ALL"]
unfixable = []

[tool.uv.sources]
vmxfp-py-db-models = { workspace = true }
vmxfp-py-utils = { workspace = true }
vmxfp-py-db-services = { workspace = true }
