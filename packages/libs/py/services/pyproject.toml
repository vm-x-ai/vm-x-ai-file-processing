[tool.coverage.run]
branch = true
source = [ "internal_services" ]

[tool.coverage.report]
exclude_lines = ['if TYPE_CHECKING:']
show_missing = true

[tool.pytest.ini_options]
addopts = """
 --cov
 --cov-report html:'../../../../coverage/packages/libs/py/services/html'
 --cov-report xml:'../../../../coverage/packages/libs/py/services/coverage.xml'
 --html='../../../../reports/packages/libs/py/services/unittests/html/index.html'
 --junitxml='../../../../reports/packages/libs/py/services/unittests/junit.xml'
"""

[project]
name = "py-services"
version = "1.0.0"
description = "Automatically generated by Nx."
requires-python = ">=3.9,<4"
readme = 'README.md'
dependencies = [
    "jinja2>=3.1.6",
    "py-db-models",
    "py-db-repositories",
]

[tool.hatch.build.targets.wheel]
packages = ["internal_services"]

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
exclude = [
  ".ruff_cache",
  ".svn",
  ".tox",
  ".venv",
  "dist",
]

line-length = 88
indent-width = 4

[tool.ruff.lint]
select = [
  # pycodestyle
  "E",
  # Pyflakes
  "F",
  # pyupgrade
  "UP",
  # flake8-bugbear
  "B",
  # flake8-simplify
  "SIM",
  # isort
  "I",
]
ignore = []

fixable = ["ALL"]
unfixable = []

[tool.uv.sources]
py-db-models = { workspace = true }
py-db-repositories = { workspace = true }
