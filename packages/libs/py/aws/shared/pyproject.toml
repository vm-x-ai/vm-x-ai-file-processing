[tool.coverage.run]
branch = true
source = [ "internal_aws_shared" ]

[tool.coverage.report]
exclude_lines = ['if TYPE_CHECKING:']
show_missing = true

[tool.pytest.ini_options]
addopts = """
 --cov
 --cov-report html:'../../../../../coverage/packages/libs/py/aws/shared/html'
 --cov-report xml:'../../../../../coverage/packages/libs/py/aws/shared/coverage.xml'
 --html='../../../../../reports/packages/libs/py/aws/shared/unittests/html/index.html'
 --junitxml='../../../../../reports/packages/libs/py/aws/shared/unittests/junit.xml'
"""

[project]
name = "py-aws-shared"
version = "1.0.0"
description = "Automatically generated by Nx."
requires-python = ">=3.9,<4"
readme = 'README.md'
dependencies = [
  "aioboto3>=14.3.0"
]

[tool.hatch.build.targets.wheel]
packages = ["internal_aws_shared"]

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
exclude = [
  ".ruff_cache",
  ".svn",
  ".tox",
  ".venv",
  "dist",
]

line-length = 88
indent-width = 4

[tool.ruff.lint]
select = [
  # pycodestyle
  "E",
  # Pyflakes
  "F",
  # pyupgrade
  "UP",
  # flake8-bugbear
  "B",
  # flake8-simplify
  "SIM",
  # isort
  "I",
]
ignore = []

fixable = ["ALL"]
unfixable = []
