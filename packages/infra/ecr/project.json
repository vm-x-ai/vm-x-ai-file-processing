{
  "name": "infra-ecr",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/infra/ecr/src",
  "projectType": "library",
  "tags": ["infra", "devops", "typescript", "no-deploy:dev", "no-deploy:prd"],
  "// targets": "to see all targets run: nx show project infra-ecr --web",
  "targets": {
    "cdk-bootstrap": {},
    "cdk-deploy": {},
    "deploy": {
      "executor": "@vmxfp/workspace-plugins:proxy-executor",
      "options": {
        "target": "cdk-deploy"
      },
      "dependsOn": ["^build"]
    }
  }
}
