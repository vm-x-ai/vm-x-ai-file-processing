{
  "name": "infra-events",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/infra/event/src",
  "projectType": "library",
  "tags": ["infra", "cdk", "typescript", "no-deploy:shared", "local-infra"],
  "// targets": "to see all targets run: nx show project infra-events --web",
  "targets": {
    "cdk-bootstrap": {},
    "cdk-deploy": {},
    "deploy": {
      "executor": "@workspace/workspace-plugins:proxy-executor",
      "options": {
        "target": "cdk-deploy"
      }
    }
  }
}
