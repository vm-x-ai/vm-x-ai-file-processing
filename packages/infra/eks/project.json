{
  "name": "infra-eks",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/infra/eks/src",
  "projectType": "library",
  "tags": ["typescript", "infra", "cdk", "shared", "no-deploy:shared"],
  "// targets": "to see all targets run: nx show project @vmxfp/infra-eks --web",
  "targets": {
    "cdk-bootstrap": {},
    "cdk-deploy": {},
    "deploy": {
      "executor": "@vmxfp/workspace-plugins:proxy-executor",
      "options": {
        "target": "cdk-deploy"
      },
      "dependsOn": [
        "^build",
        {
          "projects": "infra-network",
          "target": "deploy",
          "params": "forward"
        }
      ]
    }
  }
}
