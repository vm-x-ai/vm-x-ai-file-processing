{
  "name": "infra-github-actions",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/infra/github-actions/src",
  "projectType": "library",
  "tags": ["infra", "typescript", "devops"],
  "// targets": "to see all targets run: nx show project infra-github-actions --web",
  "targets": {
    "cdk-bootstrap": {},
    "cdk-deploy": {},
    "deploy": {
      "executor": "@workspace/workspace-plugins:proxy-executor",
      "options": {
        "target": "cdk-deploy"
      },
      "dependsOn": ["^build"]
    }
  }
}
